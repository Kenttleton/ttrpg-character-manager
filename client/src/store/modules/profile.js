// import profile from '../../api/profile'

const state = () => ({
  image: '',
  icon: 'mdi-account',
  email: 'utterback.kent.a@gmail.com',
  name: 'Kent Utterback'
})

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
